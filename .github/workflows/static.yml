# Simple workflow for deploying static content to GitHub Pages
name: Deploy static content to Pages

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["main"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Single deploy job since we're just deploying
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Upload entire repository
          path: '.'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GitHub Workflow Example</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            margin: 0;
            padding: 0;
            color: #333;
        }
        header {
            background: #24292e;
            color: white;
            padding: 1rem 0;
            text-align: center;
        }
        nav {
            display: flex;
            justify-content: center;
            background: #f5f5f5;
            padding: 0.5rem;
        }
        nav a {
            margin: 0 1rem;
            text-decoration: none;
            color: #0366d6;
        }
        main {
            padding: 2rem;
            max-width: 800px;
            margin: 0 auto;
        }
        footer {
            text-align: center;
            padding: 1rem;
            background: #f5f5f5;
            margin-top: 2rem;
        }
        .workflow-step {
            background: #f6f8fa;
            border: 1px solid #e1e4e8;
            border-radius: 6px;
            padding: 1rem;
            margin-bottom: 1rem;
        }
    </style>
</head>
<body>
    <header>
        <h1>GitHub Workflow Demo</h1>
    </header>
    
    <nav>
        <a href="#about">About</a>
        <a href="#workflow">Workflow</a>
        <a href="#contact">Contact</a>
    </nav>
    
    <main>
        <section id="about">
            <h2>About This Project</h2>
            <p>This is a simple HTML page demonstrating GitHub workflow concepts.</p>
        </section>
        
        <section id="workflow">
            <h2>Basic GitHub Workflow</h2>
            
            <div class="workflow-step">
                <h3>1. Create a Repository</h3>
                <p>Start by creating a new repository on GitHub or cloning an existing one.</p>
            </div>
            
            <div class="workflow-step">
                <h3>2. Create a Branch</h3>
                <p>Make a new branch for your feature or bugfix:</p>
                <code>git checkout -b feature-branch</code>
            </div>
            
            <div class="workflow-step">
                <h3>3. Make Changes</h3>
                <p>Edit files, add new features, or fix bugs in your branch.</p>
            </div>
            
            <div class="workflow-step">
                <h3>4. Commit Changes</h3>
                <p>Stage and commit your changes:</p>
                <code>git add .<br>git commit -m "Descriptive commit message"</code>
            </div>
            
            <div class="workflow-step">
                <h3>5. Push to GitHub</h3>
                <p>Push your branch to GitHub:</p>
                <code>git push origin feature-branch</code>
            </div>
            
            <div class="workflow-step">
                <h3>6. Create a Pull Request</h3>
                <p>On GitHub, create a pull request to merge your changes into the main branch.</p>
            </div>
        </section>
    </main>
    
    <footer id="contact">
        <p>Created with ❤️ for GitHub workflows</p>
        <p>© 2023 GitHub Demo</p>
    </footer>
</body>
</html>
